# pipeline

Ingestion, scoring, and validator — pipeline territory.

Suggested stubs (add as needed):

- `ingest/` — load / normalize raw inputs
- `score/` — referral scoring logic
- `validate/` — contract / quality checks against `/schemas`
