# data

Small samples only. Never commit the full 10k raw dump.

- Bulk / raw dumps stay local (gitignored under `data/*`).
- Tiny fixtures for demos and tests can live in `samples/` if you choose to track them.
